server {
    listen ${PORT};
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback; the app calls the API cross-origin via VITE_API_URL.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
