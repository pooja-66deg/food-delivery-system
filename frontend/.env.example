# API base URL. Leave unset in development — Vite proxies /api to the backend
# (see vite.config.ts). Set this for production builds, e.g.:
# VITE_API_URL=https://api.your-domain.com
VITE_API_URL=/api
